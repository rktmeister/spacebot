List calendar occurrences from the mirrored calendar store for a time range. Use this for calendar questions and before proposing updates or deletes so you operate on current event IDs instead of guessing from memory or email text. When replying to the user, use the returned `display_*` fields; fields ending in `_utc` are exact reference values, not the preferred user-facing render.
