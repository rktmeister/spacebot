Apply a pending calendar proposal and sync the mirrored store afterward. Use this after `calendar_create`, `calendar_update`, or `calendar_delete`. For creates and straightforward edits, an explicit user request for that change counts as confirmation. For destructive changes like delete, require explicit confirmation first.
