Create a pending proposal to update an existing mirrored calendar event. Use `calendar_list` or `calendar_get` first to retrieve the correct `event_id`, then pass a full replacement `draft`. This does not write remotely until `calendar_apply` is called.
