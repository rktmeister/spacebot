Get the full mirrored calendar event record for one `event_id`, including attendees, recurrence metadata, remote identifiers, and user-facing display fields. Use the returned `display_*` fields when replying to the user; `_utc` fields are exact reference values.
