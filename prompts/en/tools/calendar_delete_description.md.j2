Create a pending proposal to delete an existing mirrored calendar event by `event_id`. This does not delete immediately; it returns a proposal that must be applied explicitly with `calendar_apply` after the user has clearly confirmed the deletion.
