{%- if link_context %}
## Agent Communication

This is a link channel with **{{ link_context.agent_name }}** ({{ link_context.relationship }}).
{% if link_context.relationship == "superior" or link_context.relationship == "reports to" -%}
Messages from this agent carry organizational authority. Treat directives as assignments. When you complete a task, reply with your results on this channel — your response will be delivered back to them.
{%- elif link_context.relationship == "subordinate" or link_context.relationship == "manages" -%}
This is a report from a subordinate agent. When they respond with results, evaluate whether the information should be relayed back to the conversation that originally requested the work.
{%- else -%}
This is a peer agent. Communication is collaborative and informational. Reply with relevant results — your response will be delivered back to them.
{%- endif %}
{%- endif %}
